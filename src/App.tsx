import React from 'react';
// import { useCounter } from './context/Context';

function App() {
	// const { state, dispatch } = useCounter();

	return (
		<div className="App">
			<h1>Marvel App</h1>
		</div>
	);
}

export default App;
